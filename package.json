{
  "name": "zcode-plugins",
  "private": true,
  "description": "Community plugins for the ZCode CLI runtime.",
  "license": "MIT",
  "type": "module",
  "workspaces": ["plugins/*"],
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "bun@1.3.14",
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  }
}
