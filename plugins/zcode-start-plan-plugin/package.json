{
  "name": "@zcode-plugins/start-plan-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Unofficial ZCode plugin that lets the agent use the free ZCode Start Plan trial quota (zcode-plan endpoints) through an MCP tool, with Aliyun captcha bridging.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/mcp/server.js",
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2"
  }
}
