---
description: Generate with the free ZCode Start Plan trial quota.
argument-hint: "[prompt]"
skills: start-plan
---

Use the `start-plan` skill for this request:

$ARGUMENTS

The agent should first check `start_plan_status`; if an active trial exists, delegate the generation to `start_plan_generate` instead of the configured chat model.
