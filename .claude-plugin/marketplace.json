{
  "name": "zcode-plugins",
  "owner": {
    "name": "kingsword09"
  },
  "description": "Community plugins for the ZCode CLI runtime (unofficial).",
  "plugins": [
    {
      "name": "zcode-start-plan",
      "source": "./plugins/zcode-start-plan-plugin",
      "description": "Use the free ZCode Start Plan trial quota (zcode-plan endpoints) from the ZCode CLI: entitlement check, delegated generation, and one-time Aliyun captcha bridging. Requires prior `zcode login` (OAuth). Unofficial.",
      "description_i18n": {
        "en": "Use the free ZCode Start Plan trial quota (zcode-plan endpoints) from the ZCode CLI: entitlement check, delegated generation, and one-time Aliyun captcha bridging. Requires prior `zcode login` (OAuth). Unofficial.",
        "zh-CN": "在 ZCode CLI 中使用免费的 ZCode Start Plan 试用额度（zcode-plan 端点）：额度查询、委托式生成、一次性阿里云验证码桥接。需先执行 `zcode login`（OAuth）。非官方实现。"
      },
      "version": "0.1.0",
      "author": { "name": "kingsword09" },
      "category": "provider",
      "keywords": ["zai", "start-plan", "trial", "captcha", "glm", "mcp"]
    },
    {
      "name": "zcode-model-config",
      "source": "./plugins/zcode-model-config-plugin",
      "description": "Open a local web UI (React + StyleX) to edit ZCode CLI model / provider config: providers, per-model fields including multimodal toggles, main/lite model roles, with a models.dev (Vercel AI SDK) catalog for one-click import. Reads and writes the user config (~/.zcode/cli/config.json) and project config (zcode.json / .zcode/config.json). Unofficial.",
      "description_i18n": {
        "en": "Open a local web UI (React + StyleX) to edit ZCode CLI model / provider config: providers, per-model fields including multimodal toggles, main/lite model roles, with a models.dev (Vercel AI SDK) catalog for one-click import. Reads and writes the user config (~/.zcode/cli/config.json) and project config (zcode.json / .zcode/config.json). Unofficial.",
        "zh-CN": "打开本地 Web 页面（React + StyleX）编辑 ZCode CLI 的模型 / Provider 配置：Provider 管理、逐模型字段（含多模态开关）、main/lite 模型角色，内置 models.dev（Vercel AI SDK）目录一键导入。读写用户级配置（~/.zcode/cli/config.json）与项目级配置（zcode.json / .zcode/config.json）。非官方实现。"
      },
      "version": "0.1.0",
      "author": { "name": "kingsword09" },
      "category": "provider",
      "keywords": ["model", "provider", "multimodal", "models-dev", "ai-sdk", "config", "web-ui", "mcp"]
    }
  ]
}
